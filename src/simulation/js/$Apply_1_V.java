package simulation.js;

/**
 * The simulation of a functional interface with ONE parameters and V return
 * value
 *
 * @param <T>
 * @param <V> the return type
 * @author gianpiero.diblasi
 */
@FunctionalInterface
public interface $Apply_1_V<T, V> {

  V $apply(T elementT);
}
