package simulation.js;

import def.js.Array;

/**
 * Simulation of the Array object
 *
 * @author gianpiero.diblasi
 * @param <T> The type of the array
 */
public class $Array<T> extends Array<T> {

  public Array<T> concat(Array<T> array) {
    return null;
  }
}
